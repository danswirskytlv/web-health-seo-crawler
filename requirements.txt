# sitePulse — Python backend + API dependencies.
# (The UI is a separate React/Vite app under frontend/; the old Streamlit UI
#  was retired in favour of the API + React frontend.)

# --- Web Scraping & HTTP ---
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0

# CA trust bundle for live TLS verification (also a transitive dep of
# requests, pinned here because analyzer/tls.py uses it directly).
certifi==2024.8.30

# --- Environment variables ---
python-dotenv==1.0.1

# --- AI: Google Gemini ---
google-generativeai==0.8.3

# --- Reports ---
reportlab==4.2.5

# --- API layer (React frontend talks to this) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Testing ---
pytest==8.3.3
httpx==0.27.2   # FastAPI TestClient dependency
