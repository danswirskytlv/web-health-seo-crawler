[pytest]
# Where to look for tests.
testpaths = tests

# What files count as tests.
python_files = test_*.py

# Treat the project root as a source root so `from crawler ...` works.
pythonpath = .

# Show short summary of failed and errored tests at the end.
addopts = -v --tb=short
